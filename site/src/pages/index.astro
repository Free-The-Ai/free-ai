---
// P0 SEO Fix: Proper HTTP 301 redirect instead of meta refresh
// Prevents duplicate content splitting between / and /home
// Google treats 301 as a permanent redirect, preserving link equity
return Astro.redirect("/home", 301);
---
